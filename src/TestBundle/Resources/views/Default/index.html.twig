
<!DOCTYPE html>
<html lang="en">
   {% include 'TestBundle:Default:header.html.twig' %}
    <body>

        <div class="container">
            <div class="row">
                {{ form_start(form) }}
                <div class="col-md-6">


                    <div class="form-group">
                        {{ form_row(form.name) }}
                    </div>
                    <div class="form-group">
                        {{ form_row(form.email) }}
                    </div>
                    <div class="form-group">
                        {{ form_row(form.city) }}
                    </div>

                    <div class="form-group">
                        {{ form_row(form.address) }}
                    </div>
                    <div class="form-group">

                        {{ form_row(form.tags) }}
                    </div> 




                    {{ form_row(form.save) }}





                    {{ form_end(form) }}

                </div>
            </div>
        </div>

    </body>
</html> 